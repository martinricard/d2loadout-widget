services:
  - type: web
    name: d2-loadout-widget
    env: node
    plan: free
    buildCommand: cd backend && npm install
    startCommand: cd backend && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: BUNGIE_API_KEY
        sync: false
        # Set this in Render dashboard to: baadf0eb52e14b6f9a6e79dbd1f824f4